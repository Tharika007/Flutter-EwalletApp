import 'package:flutter/material.dart';

import '/../constant.dart';

class MiddleSizedLabel extends StatelessWidget {
  final Color textColor;
  final Widget? label;

  const MiddleSizedLabel({
    Key? key,
    this.textColor = primarywhite,
    this.label,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
